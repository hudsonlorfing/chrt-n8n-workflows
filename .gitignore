# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Sensitive files (credentials should never be committed)
*credentials*.json
*.env
.env.*

# Backup files
*.bak
*.backup

# Temporary sync files
.sync-temp/

# Test execution files (keep docs, ignore logs)
execution-*.json
test-results/*.log

# Debug execution output files
execution-*.json

# Debug logs directory
debug-logs/
archive/

# Sensitive patterns - NEVER commit these
**/secrets.json
**/*-credentials.json
**/*.pem
**/*.key

# Workflow files that may contain webhooks (use download for latest)
# Note: workflow files are allowed but pre-commit hook scans for secrets
